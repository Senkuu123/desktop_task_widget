# 桌面任务小组件 v2.0.1 发布说明

## 更新内容

### 修复
- 习惯末项文字偏移 — 调整字体/透明度后最后一项习惯文字位置异常
- 横向滚动条误判 — label最小宽度=容器宽度形成循环导致横向滚动条常驻
- 任务文字位置偏移 — label垂直策略改为Fixed，文字始终固定在正确位置
- 饮水日志行高被拉伸 — container固定垂直策略+末尾stretch吸收多余空间
- 饮水日志滚动条 — 内容未满4行时未隐藏，补齐pressed态样式

### 改进
- 主窗口纵/横滚动条宽度缩窄至8px（与饮水日志滚动条一致）
- 饮水日志分隔线改用独立分割线，不再误显为文字下划线
- 任务/习惯对话框风格统一（尺寸、间距、按钮颜色）
- 打包脚本构建完成后自动退出

---

## 兼容性

| 系统版本 | 支持状态 |
|---------|---------|
| Windows 11 | 支持 |
| Windows 10 | 支持 |

建议分辨率 1920x1080 及以上，支持高DPI显示器。

---

## 使用注意

- 双击 `DesktopTaskWidget.exe` 启动
- 数据文件（tasks.json / habits.json / water.json / settings.json）保存在exe同目录，定期备份
- 开机自启动可能需要管理员权限
- Windows智能应用控制拦截时：Windows安全中心 → 应用和浏览器控制 → 智能应用控制 → 关闭

---

© 2025 克里斯数据 保留所有权利
