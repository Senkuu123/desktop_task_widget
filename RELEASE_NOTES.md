# 🚀 桌面任务小组件 v1.0.0 发布说明

## 📋 主要更新内容

### 新增功能
- ✅ **智能紧急任务检测** - 任务截止时间在1小时内自动标记为紧急状态（红色P0）
- ✅ **任务归档功能** - 支持将任务归档，归档后不再显示但保留数据
- ✅ **任务完成时间记录** - 首次完成任务时自动记录完成时间
- ✅ **任务导出功能** - 支持将任务列表导出为文本文件
- ✅ **任务备份功能** - 自动创建带时间戳的备份文件

### 功能改进
- 🎨 **窗口透明度调节** - 支持10%-100%范围调节
- 📝 **字体大小调节** - 支持8pt-20pt范围调节
- 🔤 **文字透明度调节** - 支持10%-100%范围调节
- 🖥️ **高DPI显示适配** - 优化高分屏显示效果
- 🖱️ **改进窗口边缘调整** - 更精确的边界检测
- 📜 **VS Code风格滚动条** - 悬停时显示，常态隐藏

### 体验优化
- ⚡ 任务状态切换更流畅
- 🔄 任务列表自动排序（按优先级+截止时间）
- 📊 过期任务自动标记并显示警告图标
- 💾 任务数据实时自动保存

---

## 🔧 兼容性说明

| 系统版本 | 支持状态 | 备注 |
|---------|---------|------|
| Windows 11 | ✅ 完全支持 | 推荐使用 |
| Windows 10 | ✅ 完全支持 | 推荐使用 |
| Windows 8.1 | ⚠️ 基本支持 | 可能存在兼容性问题 |
| Windows 7 | ❌ 不支持 | 不兼容 |

### 运行环境要求
- .NET Framework 4.8 或更高版本
- 建议分辨率 1920×1080 及以上
- 支持高DPI显示器（100%-150%缩放）

---

## ⚠️ 使用注意事项

### 首次运行
1. 双击 `DesktopTaskWidget.exe` 启动程序
2. 首次运行会自动创建 `tasks.json` 数据文件
3. 建议首次运行时以管理员身份启动（右键→以管理员身份运行）

### 数据备份
- 任务数据存储在程序同目录的 `tasks.json` 文件中
- **强烈建议定期备份 `tasks.json` 文件**
- 程序会自动创建备份文件（格式: `tasks_backup_YYYYMMDD_HHMMSS.json`）

### 开机自启动
- 可在设置中启用开机自启动
- 如果提示权限不足，请以管理员身份运行程序
- 自启动设置存储在 Windows 注册表中

### 杀毒软件提示
- 部分杀毒软件可能误报本程序
- 建议将程序添加到杀毒软件白名单
- 本程序仅在本地运行，不连接网络，不收集任何个人信息

---

## 📁 文件结构

```
DesktopTaskWidget/
├── DesktopTaskWidget.exe    # 主程序
├── tasks.json               # 任务数据文件
├── settings.json            # 设置文件
├── tasks_backup_*.json      # 自动备份文件（自动生成）
├── tasks_export_*.txt       # 导出文件（按需生成）
└── 使用说明（请看看我）.md   # 使用说明书
```

---

## 🐛 已知问题

1. **任务栏图标显示** - 在某些系统配置下，任务栏图标可能延迟显示
2. **高DPI缩放** - 在极端缩放比例下可能存在微小的显示偏差

---

## 📧 反馈与支持

如遇到问题或有功能建议，请提供以下信息：
- 问题描述
- 重现步骤
- Windows版本
- 屏幕分辨率和缩放比例
- 错误截图（如有）

---

## 📜 许可证

本软件仅供个人非商业使用。未经授权，禁止修改、分发或用于商业目的。

---

**© 2025 Chris - 保留所有权利**